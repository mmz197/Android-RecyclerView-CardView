<?xml version="1.0" encoding="utf-8"?>
<!--
    ASSIGNMENT: Android RecyclerView and CardView Material Interface Layout
    Student Name   : Muhammad muneeb
    Student ID     : 23-ag-9627
    Generated on   : 2026-05-29
-->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recyclerviewcardview">

    <!-- CRITICAL: Internet Permission required to load Glide images from the web -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Healthy Food Hub"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
